# initialise completions with ZSH's compinit
autoload -Uz compinit && compinit

for file in ~/shell_config/*.zsh; do source "$file"; done
for file in ~/shell_functions/*.zsh; do source "$file"; done

